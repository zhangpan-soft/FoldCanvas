# FoldCanvas host project

Open this directory as a Unity project with Unity 6.3 LTS. It references the repository root as a local UPM package.

Do not implement package code under `Assets/`. The host project exists only for compilation, tests, samples, and visual verification.
